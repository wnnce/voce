// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.34.1
// source: plugin.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type ClientUnaryCall,
  type handleBidiStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "plugin.v1";

export enum DropStrategy {
  DROP_STRATEGY_UNSPECIFIED = 0,
  DROP_STRATEGY_BLOCK_IF_FULL = 1,
  DROP_STRATEGY_DROP_NEWEST = 2,
  DROP_STRATEGY_DROP_OLDEST = 3,
  UNRECOGNIZED = -1,
}

export function dropStrategyFromJSON(object: any): DropStrategy {
  switch (object) {
    case 0:
    case "DROP_STRATEGY_UNSPECIFIED":
      return DropStrategy.DROP_STRATEGY_UNSPECIFIED;
    case 1:
    case "DROP_STRATEGY_BLOCK_IF_FULL":
      return DropStrategy.DROP_STRATEGY_BLOCK_IF_FULL;
    case 2:
    case "DROP_STRATEGY_DROP_NEWEST":
      return DropStrategy.DROP_STRATEGY_DROP_NEWEST;
    case 3:
    case "DROP_STRATEGY_DROP_OLDEST":
      return DropStrategy.DROP_STRATEGY_DROP_OLDEST;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DropStrategy.UNRECOGNIZED;
  }
}

export function dropStrategyToJSON(object: DropStrategy): string {
  switch (object) {
    case DropStrategy.DROP_STRATEGY_UNSPECIFIED:
      return "DROP_STRATEGY_UNSPECIFIED";
    case DropStrategy.DROP_STRATEGY_BLOCK_IF_FULL:
      return "DROP_STRATEGY_BLOCK_IF_FULL";
    case DropStrategy.DROP_STRATEGY_DROP_NEWEST:
      return "DROP_STRATEGY_DROP_NEWEST";
    case DropStrategy.DROP_STRATEGY_DROP_OLDEST:
      return "DROP_STRATEGY_DROP_OLDEST";
    case DropStrategy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EventType {
  EVENT_TYPE_UNSPECIFIED = 0,
  EVENT_TYPE_SIGNAL = 1,
  EVENT_TYPE_PAYLOAD = 2,
  EVENT_TYPE_AUDIO = 3,
  EVENT_TYPE_VIDEO = 4,
  UNRECOGNIZED = -1,
}

export function eventTypeFromJSON(object: any): EventType {
  switch (object) {
    case 0:
    case "EVENT_TYPE_UNSPECIFIED":
      return EventType.EVENT_TYPE_UNSPECIFIED;
    case 1:
    case "EVENT_TYPE_SIGNAL":
      return EventType.EVENT_TYPE_SIGNAL;
    case 2:
    case "EVENT_TYPE_PAYLOAD":
      return EventType.EVENT_TYPE_PAYLOAD;
    case 3:
    case "EVENT_TYPE_AUDIO":
      return EventType.EVENT_TYPE_AUDIO;
    case 4:
    case "EVENT_TYPE_VIDEO":
      return EventType.EVENT_TYPE_VIDEO;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EventType.UNRECOGNIZED;
  }
}

export function eventTypeToJSON(object: EventType): string {
  switch (object) {
    case EventType.EVENT_TYPE_UNSPECIFIED:
      return "EVENT_TYPE_UNSPECIFIED";
    case EventType.EVENT_TYPE_SIGNAL:
      return "EVENT_TYPE_SIGNAL";
    case EventType.EVENT_TYPE_PAYLOAD:
      return "EVENT_TYPE_PAYLOAD";
    case EventType.EVENT_TYPE_AUDIO:
      return "EVENT_TYPE_AUDIO";
    case EventType.EVENT_TYPE_VIDEO:
      return "EVENT_TYPE_VIDEO";
    case EventType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ValueType {
  VALUE_TYPE_UNSPECIFIED = 0,
  VALUE_TYPE_STRING = 1,
  VALUE_TYPE_NUMBER = 2,
  VALUE_TYPE_INTEGER = 3,
  VALUE_TYPE_BOOLEAN = 4,
  VALUE_TYPE_OBJECT = 5,
  VALUE_TYPE_ARRAY = 6,
  UNRECOGNIZED = -1,
}

export function valueTypeFromJSON(object: any): ValueType {
  switch (object) {
    case 0:
    case "VALUE_TYPE_UNSPECIFIED":
      return ValueType.VALUE_TYPE_UNSPECIFIED;
    case 1:
    case "VALUE_TYPE_STRING":
      return ValueType.VALUE_TYPE_STRING;
    case 2:
    case "VALUE_TYPE_NUMBER":
      return ValueType.VALUE_TYPE_NUMBER;
    case 3:
    case "VALUE_TYPE_INTEGER":
      return ValueType.VALUE_TYPE_INTEGER;
    case 4:
    case "VALUE_TYPE_BOOLEAN":
      return ValueType.VALUE_TYPE_BOOLEAN;
    case 5:
    case "VALUE_TYPE_OBJECT":
      return ValueType.VALUE_TYPE_OBJECT;
    case 6:
    case "VALUE_TYPE_ARRAY":
      return ValueType.VALUE_TYPE_ARRAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ValueType.UNRECOGNIZED;
  }
}

export function valueTypeToJSON(object: ValueType): string {
  switch (object) {
    case ValueType.VALUE_TYPE_UNSPECIFIED:
      return "VALUE_TYPE_UNSPECIFIED";
    case ValueType.VALUE_TYPE_STRING:
      return "VALUE_TYPE_STRING";
    case ValueType.VALUE_TYPE_NUMBER:
      return "VALUE_TYPE_NUMBER";
    case ValueType.VALUE_TYPE_INTEGER:
      return "VALUE_TYPE_INTEGER";
    case ValueType.VALUE_TYPE_BOOLEAN:
      return "VALUE_TYPE_BOOLEAN";
    case ValueType.VALUE_TYPE_OBJECT:
      return "VALUE_TYPE_OBJECT";
    case ValueType.VALUE_TYPE_ARRAY:
      return "VALUE_TYPE_ARRAY";
    case ValueType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RuntimeMessageType {
  RUNTIME_MESSAGE_TYPE_UNSPECIFIED = 0,
  RUNTIME_MESSAGE_TYPE_LIFECYCLE = 1,
  RUNTIME_MESSAGE_TYPE_SIGNAL = 2,
  RUNTIME_MESSAGE_TYPE_PAYLOAD = 3,
  RUNTIME_MESSAGE_TYPE_EMIT_SIGNAL = 4,
  RUNTIME_MESSAGE_TYPE_EMIT_PAYLOAD = 5,
  RUNTIME_MESSAGE_TYPE_EMIT_LOG = 6,
  RUNTIME_MESSAGE_TYPE_ACK = 7,
  RUNTIME_MESSAGE_TYPE_REPORT = 8,
  RUNTIME_MESSAGE_TYPE_CANCEL = 9,
  UNRECOGNIZED = -1,
}

export function runtimeMessageTypeFromJSON(object: any): RuntimeMessageType {
  switch (object) {
    case 0:
    case "RUNTIME_MESSAGE_TYPE_UNSPECIFIED":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_UNSPECIFIED;
    case 1:
    case "RUNTIME_MESSAGE_TYPE_LIFECYCLE":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_LIFECYCLE;
    case 2:
    case "RUNTIME_MESSAGE_TYPE_SIGNAL":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_SIGNAL;
    case 3:
    case "RUNTIME_MESSAGE_TYPE_PAYLOAD":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_PAYLOAD;
    case 4:
    case "RUNTIME_MESSAGE_TYPE_EMIT_SIGNAL":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_EMIT_SIGNAL;
    case 5:
    case "RUNTIME_MESSAGE_TYPE_EMIT_PAYLOAD":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_EMIT_PAYLOAD;
    case 6:
    case "RUNTIME_MESSAGE_TYPE_EMIT_LOG":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_EMIT_LOG;
    case 7:
    case "RUNTIME_MESSAGE_TYPE_ACK":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_ACK;
    case 8:
    case "RUNTIME_MESSAGE_TYPE_REPORT":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_REPORT;
    case 9:
    case "RUNTIME_MESSAGE_TYPE_CANCEL":
      return RuntimeMessageType.RUNTIME_MESSAGE_TYPE_CANCEL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RuntimeMessageType.UNRECOGNIZED;
  }
}

export function runtimeMessageTypeToJSON(object: RuntimeMessageType): string {
  switch (object) {
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_UNSPECIFIED:
      return "RUNTIME_MESSAGE_TYPE_UNSPECIFIED";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_LIFECYCLE:
      return "RUNTIME_MESSAGE_TYPE_LIFECYCLE";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_SIGNAL:
      return "RUNTIME_MESSAGE_TYPE_SIGNAL";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_PAYLOAD:
      return "RUNTIME_MESSAGE_TYPE_PAYLOAD";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_EMIT_SIGNAL:
      return "RUNTIME_MESSAGE_TYPE_EMIT_SIGNAL";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_EMIT_PAYLOAD:
      return "RUNTIME_MESSAGE_TYPE_EMIT_PAYLOAD";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_EMIT_LOG:
      return "RUNTIME_MESSAGE_TYPE_EMIT_LOG";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_ACK:
      return "RUNTIME_MESSAGE_TYPE_ACK";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_REPORT:
      return "RUNTIME_MESSAGE_TYPE_REPORT";
    case RuntimeMessageType.RUNTIME_MESSAGE_TYPE_CANCEL:
      return "RUNTIME_MESSAGE_TYPE_CANCEL";
    case RuntimeMessageType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum LifecycleType {
  LIFECYCLE_TYPE_UNSPECIFIED = 0,
  LIFECYCLE_TYPE_START = 1,
  LIFECYCLE_TYPE_READY = 2,
  LIFECYCLE_TYPE_PAUSE = 3,
  LIFECYCLE_TYPE_RESUME = 4,
  LIFECYCLE_TYPE_STOP = 5,
  UNRECOGNIZED = -1,
}

export function lifecycleTypeFromJSON(object: any): LifecycleType {
  switch (object) {
    case 0:
    case "LIFECYCLE_TYPE_UNSPECIFIED":
      return LifecycleType.LIFECYCLE_TYPE_UNSPECIFIED;
    case 1:
    case "LIFECYCLE_TYPE_START":
      return LifecycleType.LIFECYCLE_TYPE_START;
    case 2:
    case "LIFECYCLE_TYPE_READY":
      return LifecycleType.LIFECYCLE_TYPE_READY;
    case 3:
    case "LIFECYCLE_TYPE_PAUSE":
      return LifecycleType.LIFECYCLE_TYPE_PAUSE;
    case 4:
    case "LIFECYCLE_TYPE_RESUME":
      return LifecycleType.LIFECYCLE_TYPE_RESUME;
    case 5:
    case "LIFECYCLE_TYPE_STOP":
      return LifecycleType.LIFECYCLE_TYPE_STOP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LifecycleType.UNRECOGNIZED;
  }
}

export function lifecycleTypeToJSON(object: LifecycleType): string {
  switch (object) {
    case LifecycleType.LIFECYCLE_TYPE_UNSPECIFIED:
      return "LIFECYCLE_TYPE_UNSPECIFIED";
    case LifecycleType.LIFECYCLE_TYPE_START:
      return "LIFECYCLE_TYPE_START";
    case LifecycleType.LIFECYCLE_TYPE_READY:
      return "LIFECYCLE_TYPE_READY";
    case LifecycleType.LIFECYCLE_TYPE_PAUSE:
      return "LIFECYCLE_TYPE_PAUSE";
    case LifecycleType.LIFECYCLE_TYPE_RESUME:
      return "LIFECYCLE_TYPE_RESUME";
    case LifecycleType.LIFECYCLE_TYPE_STOP:
      return "LIFECYCLE_TYPE_STOP";
    case LifecycleType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum LogLevel {
  LOG_LEVEL_UNSPECIFIED = 0,
  LOG_LEVEL_DEBUG = 1,
  LOG_LEVEL_INFO = 2,
  LOG_LEVEL_WARN = 3,
  LOG_LEVEL_ERROR = 4,
  UNRECOGNIZED = -1,
}

export function logLevelFromJSON(object: any): LogLevel {
  switch (object) {
    case 0:
    case "LOG_LEVEL_UNSPECIFIED":
      return LogLevel.LOG_LEVEL_UNSPECIFIED;
    case 1:
    case "LOG_LEVEL_DEBUG":
      return LogLevel.LOG_LEVEL_DEBUG;
    case 2:
    case "LOG_LEVEL_INFO":
      return LogLevel.LOG_LEVEL_INFO;
    case 3:
    case "LOG_LEVEL_WARN":
      return LogLevel.LOG_LEVEL_WARN;
    case 4:
    case "LOG_LEVEL_ERROR":
      return LogLevel.LOG_LEVEL_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LogLevel.UNRECOGNIZED;
  }
}

export function logLevelToJSON(object: LogLevel): string {
  switch (object) {
    case LogLevel.LOG_LEVEL_UNSPECIFIED:
      return "LOG_LEVEL_UNSPECIFIED";
    case LogLevel.LOG_LEVEL_DEBUG:
      return "LOG_LEVEL_DEBUG";
    case LogLevel.LOG_LEVEL_INFO:
      return "LOG_LEVEL_INFO";
    case LogLevel.LOG_LEVEL_WARN:
      return "LOG_LEVEL_WARN";
    case LogLevel.LOG_LEVEL_ERROR:
      return "LOG_LEVEL_ERROR";
    case LogLevel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ReportStatus {
  REPORT_STATUS_UNSPECIFIED = 0,
  REPORT_STATUS_OK = 1,
  REPORT_STATUS_ERROR = 2,
  REPORT_STATUS_CANCELED = 3,
  UNRECOGNIZED = -1,
}

export function reportStatusFromJSON(object: any): ReportStatus {
  switch (object) {
    case 0:
    case "REPORT_STATUS_UNSPECIFIED":
      return ReportStatus.REPORT_STATUS_UNSPECIFIED;
    case 1:
    case "REPORT_STATUS_OK":
      return ReportStatus.REPORT_STATUS_OK;
    case 2:
    case "REPORT_STATUS_ERROR":
      return ReportStatus.REPORT_STATUS_ERROR;
    case 3:
    case "REPORT_STATUS_CANCELED":
      return ReportStatus.REPORT_STATUS_CANCELED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReportStatus.UNRECOGNIZED;
  }
}

export function reportStatusToJSON(object: ReportStatus): string {
  switch (object) {
    case ReportStatus.REPORT_STATUS_UNSPECIFIED:
      return "REPORT_STATUS_UNSPECIFIED";
    case ReportStatus.REPORT_STATUS_OK:
      return "REPORT_STATUS_OK";
    case ReportStatus.REPORT_STATUS_ERROR:
      return "REPORT_STATUS_ERROR";
    case ReportStatus.REPORT_STATUS_CANCELED:
      return "REPORT_STATUS_CANCELED";
    case ReportStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface PingRequest {
  clientId: string;
}

export interface PingResponse {
  serverId: string;
  version: string;
}

export interface ListPluginsRequest {
  namespace: string;
}

export interface ListPluginsResponse {
  plugins: PluginMetadata[];
}

export interface CreateInstanceRequest {
  instanceId: string;
  pluginName: string;
  config: Buffer;
  metadata: { [key: string]: string };
}

export interface CreateInstanceRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface CreateInstanceResponse {
  instanceId: string;
}

export interface DestroyInstanceRequest {
  instanceId: string;
  reason: string;
}

export interface DestroyInstanceResponse {
}

export interface PluginMetadata {
  name: string;
  description: string;
  schema: string;
  inputs: Property[];
  outputs: Property[];
  ports: PortMetadata[];
  multiTrack: MultiTrackConfig | undefined;
}

export interface MultiTrackConfig {
  enabled: boolean;
  payload: TrackConfig | undefined;
}

export interface TrackConfig {
  enabled: boolean;
  bufferSize: number;
  dropStrategy: DropStrategy;
  interruptSignals: string[];
}

export interface Property {
  type: EventType;
  name: string;
  fields: Field[];
}

export interface Field {
  key: string;
  type: ValueType;
  required: boolean;
}

export interface PortMetadata {
  type: EventType;
  port: number;
  name: string;
  description: string;
}

export interface RuntimeMessage {
  instanceId: string;
  messageId: string;
  correlationId: string;
  type: RuntimeMessageType;
  metadata: { [key: string]: string };
  body:
    | { $case: "lifecycle"; lifecycle: LifecycleEvent }
    | { $case: "signal"; signal: SignalEvent }
    | { $case: "payload"; payload: PayloadEvent }
    | { $case: "emitSignal"; emitSignal: EmitSignal }
    | { $case: "emitPayload"; emitPayload: EmitPayload }
    | { $case: "emitLog"; emitLog: EmitLog }
    | { $case: "report"; report: EventReport }
    | { $case: "ack"; ack: EventAck }
    | { $case: "cancel"; cancel: CancelEvent }
    | undefined;
}

export interface RuntimeMessage_MetadataEntry {
  key: string;
  value: string;
}

export interface LifecycleEvent {
  type: LifecycleType;
}

export interface SignalEvent {
  name: string;
  properties: Buffer;
}

export interface PayloadEvent {
  name: string;
  properties: Buffer;
}

export interface EmitSignal {
  signal: SignalEvent | undefined;
  port: number;
}

export interface EmitPayload {
  payload: PayloadEvent | undefined;
  port: number;
}

export interface EmitLog {
  level: LogLevel;
  message: string;
  fields: { [key: string]: string };
}

export interface EmitLog_FieldsEntry {
  key: string;
  value: string;
}

export interface EventReport {
  status: ReportStatus;
  error: RemoteError | undefined;
}

export interface RemoteError {
  code: string;
  message: string;
  details: string;
}

export interface EventAck {
  timestamp: number;
}

export interface CancelEvent {
}

function createBasePingRequest(): PingRequest {
  return { clientId: "" };
}

export const PingRequest: MessageFns<PingRequest> = {
  encode(message: PingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientId !== "") {
      writer.uint32(10).string(message.clientId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingRequest {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
    };
  },

  toJSON(message: PingRequest): unknown {
    const obj: any = {};
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    return obj;
  },

  create(base?: DeepPartial<PingRequest>): PingRequest {
    return PingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PingRequest>): PingRequest {
    const message = createBasePingRequest();
    message.clientId = object.clientId ?? "";
    return message;
  },
};

function createBasePingResponse(): PingResponse {
  return { serverId: "", version: "" };
}

export const PingResponse: MessageFns<PingResponse> = {
  encode(message: PingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverId !== "") {
      writer.uint32(10).string(message.serverId);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingResponse {
    return {
      serverId: isSet(object.serverId)
        ? globalThis.String(object.serverId)
        : isSet(object.server_id)
        ? globalThis.String(object.server_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: PingResponse): unknown {
    const obj: any = {};
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create(base?: DeepPartial<PingResponse>): PingResponse {
    return PingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PingResponse>): PingResponse {
    const message = createBasePingResponse();
    message.serverId = object.serverId ?? "";
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseListPluginsRequest(): ListPluginsRequest {
  return { namespace: "" };
}

export const ListPluginsRequest: MessageFns<ListPluginsRequest> = {
  encode(message: ListPluginsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPluginsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPluginsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPluginsRequest {
    return { namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "" };
  },

  toJSON(message: ListPluginsRequest): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    return obj;
  },

  create(base?: DeepPartial<ListPluginsRequest>): ListPluginsRequest {
    return ListPluginsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPluginsRequest>): ListPluginsRequest {
    const message = createBaseListPluginsRequest();
    message.namespace = object.namespace ?? "";
    return message;
  },
};

function createBaseListPluginsResponse(): ListPluginsResponse {
  return { plugins: [] };
}

export const ListPluginsResponse: MessageFns<ListPluginsResponse> = {
  encode(message: ListPluginsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.plugins) {
      PluginMetadata.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPluginsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPluginsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.plugins.push(PluginMetadata.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPluginsResponse {
    return {
      plugins: globalThis.Array.isArray(object?.plugins)
        ? object.plugins.map((e: any) => PluginMetadata.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListPluginsResponse): unknown {
    const obj: any = {};
    if (message.plugins?.length) {
      obj.plugins = message.plugins.map((e) => PluginMetadata.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListPluginsResponse>): ListPluginsResponse {
    return ListPluginsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPluginsResponse>): ListPluginsResponse {
    const message = createBaseListPluginsResponse();
    message.plugins = object.plugins?.map((e) => PluginMetadata.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateInstanceRequest(): CreateInstanceRequest {
  return { instanceId: "", pluginName: "", config: Buffer.alloc(0), metadata: {} };
}

export const CreateInstanceRequest: MessageFns<CreateInstanceRequest> = {
  encode(message: CreateInstanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.pluginName !== "") {
      writer.uint32(18).string(message.pluginName);
    }
    if (message.config.length !== 0) {
      writer.uint32(26).bytes(message.config);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      CreateInstanceRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateInstanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateInstanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pluginName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.config = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = CreateInstanceRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.metadata[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateInstanceRequest {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      pluginName: isSet(object.pluginName)
        ? globalThis.String(object.pluginName)
        : isSet(object.plugin_name)
        ? globalThis.String(object.plugin_name)
        : "",
      config: isSet(object.config) ? Buffer.from(bytesFromBase64(object.config)) : Buffer.alloc(0),
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: CreateInstanceRequest): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.pluginName !== "") {
      obj.pluginName = message.pluginName;
    }
    if (message.config.length !== 0) {
      obj.config = base64FromBytes(message.config);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<CreateInstanceRequest>): CreateInstanceRequest {
    return CreateInstanceRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateInstanceRequest>): CreateInstanceRequest {
    const message = createBaseCreateInstanceRequest();
    message.instanceId = object.instanceId ?? "";
    message.pluginName = object.pluginName ?? "";
    message.config = object.config ?? Buffer.alloc(0);
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCreateInstanceRequest_MetadataEntry(): CreateInstanceRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const CreateInstanceRequest_MetadataEntry: MessageFns<CreateInstanceRequest_MetadataEntry> = {
  encode(message: CreateInstanceRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateInstanceRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateInstanceRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateInstanceRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateInstanceRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateInstanceRequest_MetadataEntry>): CreateInstanceRequest_MetadataEntry {
    return CreateInstanceRequest_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateInstanceRequest_MetadataEntry>): CreateInstanceRequest_MetadataEntry {
    const message = createBaseCreateInstanceRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateInstanceResponse(): CreateInstanceResponse {
  return { instanceId: "" };
}

export const CreateInstanceResponse: MessageFns<CreateInstanceResponse> = {
  encode(message: CreateInstanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateInstanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateInstanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateInstanceResponse {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
    };
  },

  toJSON(message: CreateInstanceResponse): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateInstanceResponse>): CreateInstanceResponse {
    return CreateInstanceResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateInstanceResponse>): CreateInstanceResponse {
    const message = createBaseCreateInstanceResponse();
    message.instanceId = object.instanceId ?? "";
    return message;
  },
};

function createBaseDestroyInstanceRequest(): DestroyInstanceRequest {
  return { instanceId: "", reason: "" };
}

export const DestroyInstanceRequest: MessageFns<DestroyInstanceRequest> = {
  encode(message: DestroyInstanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DestroyInstanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDestroyInstanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DestroyInstanceRequest {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: DestroyInstanceRequest): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<DestroyInstanceRequest>): DestroyInstanceRequest {
    return DestroyInstanceRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DestroyInstanceRequest>): DestroyInstanceRequest {
    const message = createBaseDestroyInstanceRequest();
    message.instanceId = object.instanceId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseDestroyInstanceResponse(): DestroyInstanceResponse {
  return {};
}

export const DestroyInstanceResponse: MessageFns<DestroyInstanceResponse> = {
  encode(_: DestroyInstanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DestroyInstanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDestroyInstanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DestroyInstanceResponse {
    return {};
  },

  toJSON(_: DestroyInstanceResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<DestroyInstanceResponse>): DestroyInstanceResponse {
    return DestroyInstanceResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<DestroyInstanceResponse>): DestroyInstanceResponse {
    const message = createBaseDestroyInstanceResponse();
    return message;
  },
};

function createBasePluginMetadata(): PluginMetadata {
  return { name: "", description: "", schema: "", inputs: [], outputs: [], ports: [], multiTrack: undefined };
}

export const PluginMetadata: MessageFns<PluginMetadata> = {
  encode(message: PluginMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.schema !== "") {
      writer.uint32(34).string(message.schema);
    }
    for (const v of message.inputs) {
      Property.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.outputs) {
      Property.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.ports) {
      PortMetadata.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.multiTrack !== undefined) {
      MultiTrackConfig.encode(message.multiTrack, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PluginMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePluginMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.schema = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.inputs.push(Property.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.outputs.push(Property.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.ports.push(PortMetadata.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.multiTrack = MultiTrackConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PluginMetadata {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      schema: isSet(object.schema) ? globalThis.String(object.schema) : "",
      inputs: globalThis.Array.isArray(object?.inputs) ? object.inputs.map((e: any) => Property.fromJSON(e)) : [],
      outputs: globalThis.Array.isArray(object?.outputs) ? object.outputs.map((e: any) => Property.fromJSON(e)) : [],
      ports: globalThis.Array.isArray(object?.ports) ? object.ports.map((e: any) => PortMetadata.fromJSON(e)) : [],
      multiTrack: isSet(object.multiTrack)
        ? MultiTrackConfig.fromJSON(object.multiTrack)
        : isSet(object.multi_track)
        ? MultiTrackConfig.fromJSON(object.multi_track)
        : undefined,
    };
  },

  toJSON(message: PluginMetadata): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.schema !== "") {
      obj.schema = message.schema;
    }
    if (message.inputs?.length) {
      obj.inputs = message.inputs.map((e) => Property.toJSON(e));
    }
    if (message.outputs?.length) {
      obj.outputs = message.outputs.map((e) => Property.toJSON(e));
    }
    if (message.ports?.length) {
      obj.ports = message.ports.map((e) => PortMetadata.toJSON(e));
    }
    if (message.multiTrack !== undefined) {
      obj.multiTrack = MultiTrackConfig.toJSON(message.multiTrack);
    }
    return obj;
  },

  create(base?: DeepPartial<PluginMetadata>): PluginMetadata {
    return PluginMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PluginMetadata>): PluginMetadata {
    const message = createBasePluginMetadata();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.schema = object.schema ?? "";
    message.inputs = object.inputs?.map((e) => Property.fromPartial(e)) || [];
    message.outputs = object.outputs?.map((e) => Property.fromPartial(e)) || [];
    message.ports = object.ports?.map((e) => PortMetadata.fromPartial(e)) || [];
    message.multiTrack = (object.multiTrack !== undefined && object.multiTrack !== null)
      ? MultiTrackConfig.fromPartial(object.multiTrack)
      : undefined;
    return message;
  },
};

function createBaseMultiTrackConfig(): MultiTrackConfig {
  return { enabled: false, payload: undefined };
}

export const MultiTrackConfig: MessageFns<MultiTrackConfig> = {
  encode(message: MultiTrackConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.payload !== undefined) {
      TrackConfig.encode(message.payload, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultiTrackConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultiTrackConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = TrackConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultiTrackConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      payload: isSet(object.payload) ? TrackConfig.fromJSON(object.payload) : undefined,
    };
  },

  toJSON(message: MultiTrackConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.payload !== undefined) {
      obj.payload = TrackConfig.toJSON(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<MultiTrackConfig>): MultiTrackConfig {
    return MultiTrackConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MultiTrackConfig>): MultiTrackConfig {
    const message = createBaseMultiTrackConfig();
    message.enabled = object.enabled ?? false;
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? TrackConfig.fromPartial(object.payload)
      : undefined;
    return message;
  },
};

function createBaseTrackConfig(): TrackConfig {
  return { enabled: false, bufferSize: 0, dropStrategy: 0, interruptSignals: [] };
}

export const TrackConfig: MessageFns<TrackConfig> = {
  encode(message: TrackConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.bufferSize !== 0) {
      writer.uint32(16).int32(message.bufferSize);
    }
    if (message.dropStrategy !== 0) {
      writer.uint32(24).int32(message.dropStrategy);
    }
    for (const v of message.interruptSignals) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bufferSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dropStrategy = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.interruptSignals.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackConfig {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      bufferSize: isSet(object.bufferSize)
        ? globalThis.Number(object.bufferSize)
        : isSet(object.buffer_size)
        ? globalThis.Number(object.buffer_size)
        : 0,
      dropStrategy: isSet(object.dropStrategy)
        ? dropStrategyFromJSON(object.dropStrategy)
        : isSet(object.drop_strategy)
        ? dropStrategyFromJSON(object.drop_strategy)
        : 0,
      interruptSignals: globalThis.Array.isArray(object?.interruptSignals)
        ? object.interruptSignals.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.interrupt_signals)
        ? object.interrupt_signals.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: TrackConfig): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.bufferSize !== 0) {
      obj.bufferSize = Math.round(message.bufferSize);
    }
    if (message.dropStrategy !== 0) {
      obj.dropStrategy = dropStrategyToJSON(message.dropStrategy);
    }
    if (message.interruptSignals?.length) {
      obj.interruptSignals = message.interruptSignals;
    }
    return obj;
  },

  create(base?: DeepPartial<TrackConfig>): TrackConfig {
    return TrackConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TrackConfig>): TrackConfig {
    const message = createBaseTrackConfig();
    message.enabled = object.enabled ?? false;
    message.bufferSize = object.bufferSize ?? 0;
    message.dropStrategy = object.dropStrategy ?? 0;
    message.interruptSignals = object.interruptSignals?.map((e) => e) || [];
    return message;
  },
};

function createBaseProperty(): Property {
  return { type: 0, name: "", fields: [] };
}

export const Property: MessageFns<Property> = {
  encode(message: Property, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.fields) {
      Field.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Property {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProperty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fields.push(Field.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Property {
    return {
      type: isSet(object.type) ? eventTypeFromJSON(object.type) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      fields: globalThis.Array.isArray(object?.fields) ? object.fields.map((e: any) => Field.fromJSON(e)) : [],
    };
  },

  toJSON(message: Property): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = eventTypeToJSON(message.type);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.fields?.length) {
      obj.fields = message.fields.map((e) => Field.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Property>): Property {
    return Property.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Property>): Property {
    const message = createBaseProperty();
    message.type = object.type ?? 0;
    message.name = object.name ?? "";
    message.fields = object.fields?.map((e) => Field.fromPartial(e)) || [];
    return message;
  },
};

function createBaseField(): Field {
  return { key: "", type: 0, required: false };
}

export const Field: MessageFns<Field> = {
  encode(message: Field, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.required !== false) {
      writer.uint32(24).bool(message.required);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Field {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseField();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Field {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      type: isSet(object.type) ? valueTypeFromJSON(object.type) : 0,
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
    };
  },

  toJSON(message: Field): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.type !== 0) {
      obj.type = valueTypeToJSON(message.type);
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    return obj;
  },

  create(base?: DeepPartial<Field>): Field {
    return Field.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Field>): Field {
    const message = createBaseField();
    message.key = object.key ?? "";
    message.type = object.type ?? 0;
    message.required = object.required ?? false;
    return message;
  },
};

function createBasePortMetadata(): PortMetadata {
  return { type: 0, port: 0, name: "", description: "" };
}

export const PortMetadata: MessageFns<PortMetadata> = {
  encode(message: PortMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.port !== 0) {
      writer.uint32(16).int32(message.port);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortMetadata {
    return {
      type: isSet(object.type) ? eventTypeFromJSON(object.type) : 0,
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: PortMetadata): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = eventTypeToJSON(message.type);
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<PortMetadata>): PortMetadata {
    return PortMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PortMetadata>): PortMetadata {
    const message = createBasePortMetadata();
    message.type = object.type ?? 0;
    message.port = object.port ?? 0;
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseRuntimeMessage(): RuntimeMessage {
  return { instanceId: "", messageId: "", correlationId: "", type: 0, metadata: {}, body: undefined };
}

export const RuntimeMessage: MessageFns<RuntimeMessage> = {
  encode(message: RuntimeMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.messageId !== "") {
      writer.uint32(18).string(message.messageId);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      RuntimeMessage_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    switch (message.body?.$case) {
      case "lifecycle":
        LifecycleEvent.encode(message.body.lifecycle, writer.uint32(82).fork()).join();
        break;
      case "signal":
        SignalEvent.encode(message.body.signal, writer.uint32(90).fork()).join();
        break;
      case "payload":
        PayloadEvent.encode(message.body.payload, writer.uint32(98).fork()).join();
        break;
      case "emitSignal":
        EmitSignal.encode(message.body.emitSignal, writer.uint32(106).fork()).join();
        break;
      case "emitPayload":
        EmitPayload.encode(message.body.emitPayload, writer.uint32(114).fork()).join();
        break;
      case "emitLog":
        EmitLog.encode(message.body.emitLog, writer.uint32(122).fork()).join();
        break;
      case "report":
        EventReport.encode(message.body.report, writer.uint32(130).fork()).join();
        break;
      case "ack":
        EventAck.encode(message.body.ack, writer.uint32(138).fork()).join();
        break;
      case "cancel":
        CancelEvent.encode(message.body.cancel, writer.uint32(146).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuntimeMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuntimeMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = RuntimeMessage_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.body = { $case: "lifecycle", lifecycle: LifecycleEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.body = { $case: "signal", signal: SignalEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.body = { $case: "payload", payload: PayloadEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.body = { $case: "emitSignal", emitSignal: EmitSignal.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.body = { $case: "emitPayload", emitPayload: EmitPayload.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.body = { $case: "emitLog", emitLog: EmitLog.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.body = { $case: "report", report: EventReport.decode(reader, reader.uint32()) };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.body = { $case: "ack", ack: EventAck.decode(reader, reader.uint32()) };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.body = { $case: "cancel", cancel: CancelEvent.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuntimeMessage {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      type: isSet(object.type) ? runtimeMessageTypeFromJSON(object.type) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      body: isSet(object.lifecycle)
        ? { $case: "lifecycle", lifecycle: LifecycleEvent.fromJSON(object.lifecycle) }
        : isSet(object.signal)
        ? { $case: "signal", signal: SignalEvent.fromJSON(object.signal) }
        : isSet(object.payload)
        ? { $case: "payload", payload: PayloadEvent.fromJSON(object.payload) }
        : isSet(object.emitSignal)
        ? { $case: "emitSignal", emitSignal: EmitSignal.fromJSON(object.emitSignal) }
        : isSet(object.emit_signal)
        ? { $case: "emitSignal", emitSignal: EmitSignal.fromJSON(object.emit_signal) }
        : isSet(object.emitPayload)
        ? { $case: "emitPayload", emitPayload: EmitPayload.fromJSON(object.emitPayload) }
        : isSet(object.emit_payload)
        ? { $case: "emitPayload", emitPayload: EmitPayload.fromJSON(object.emit_payload) }
        : isSet(object.emitLog)
        ? { $case: "emitLog", emitLog: EmitLog.fromJSON(object.emitLog) }
        : isSet(object.emit_log)
        ? { $case: "emitLog", emitLog: EmitLog.fromJSON(object.emit_log) }
        : isSet(object.report)
        ? { $case: "report", report: EventReport.fromJSON(object.report) }
        : isSet(object.ack)
        ? { $case: "ack", ack: EventAck.fromJSON(object.ack) }
        : isSet(object.cancel)
        ? { $case: "cancel", cancel: CancelEvent.fromJSON(object.cancel) }
        : undefined,
    };
  },

  toJSON(message: RuntimeMessage): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.type !== 0) {
      obj.type = runtimeMessageTypeToJSON(message.type);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.body?.$case === "lifecycle") {
      obj.lifecycle = LifecycleEvent.toJSON(message.body.lifecycle);
    } else if (message.body?.$case === "signal") {
      obj.signal = SignalEvent.toJSON(message.body.signal);
    } else if (message.body?.$case === "payload") {
      obj.payload = PayloadEvent.toJSON(message.body.payload);
    } else if (message.body?.$case === "emitSignal") {
      obj.emitSignal = EmitSignal.toJSON(message.body.emitSignal);
    } else if (message.body?.$case === "emitPayload") {
      obj.emitPayload = EmitPayload.toJSON(message.body.emitPayload);
    } else if (message.body?.$case === "emitLog") {
      obj.emitLog = EmitLog.toJSON(message.body.emitLog);
    } else if (message.body?.$case === "report") {
      obj.report = EventReport.toJSON(message.body.report);
    } else if (message.body?.$case === "ack") {
      obj.ack = EventAck.toJSON(message.body.ack);
    } else if (message.body?.$case === "cancel") {
      obj.cancel = CancelEvent.toJSON(message.body.cancel);
    }
    return obj;
  },

  create(base?: DeepPartial<RuntimeMessage>): RuntimeMessage {
    return RuntimeMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RuntimeMessage>): RuntimeMessage {
    const message = createBaseRuntimeMessage();
    message.instanceId = object.instanceId ?? "";
    message.messageId = object.messageId ?? "";
    message.correlationId = object.correlationId ?? "";
    message.type = object.type ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    switch (object.body?.$case) {
      case "lifecycle": {
        if (object.body?.lifecycle !== undefined && object.body?.lifecycle !== null) {
          message.body = { $case: "lifecycle", lifecycle: LifecycleEvent.fromPartial(object.body.lifecycle) };
        }
        break;
      }
      case "signal": {
        if (object.body?.signal !== undefined && object.body?.signal !== null) {
          message.body = { $case: "signal", signal: SignalEvent.fromPartial(object.body.signal) };
        }
        break;
      }
      case "payload": {
        if (object.body?.payload !== undefined && object.body?.payload !== null) {
          message.body = { $case: "payload", payload: PayloadEvent.fromPartial(object.body.payload) };
        }
        break;
      }
      case "emitSignal": {
        if (object.body?.emitSignal !== undefined && object.body?.emitSignal !== null) {
          message.body = { $case: "emitSignal", emitSignal: EmitSignal.fromPartial(object.body.emitSignal) };
        }
        break;
      }
      case "emitPayload": {
        if (object.body?.emitPayload !== undefined && object.body?.emitPayload !== null) {
          message.body = { $case: "emitPayload", emitPayload: EmitPayload.fromPartial(object.body.emitPayload) };
        }
        break;
      }
      case "emitLog": {
        if (object.body?.emitLog !== undefined && object.body?.emitLog !== null) {
          message.body = { $case: "emitLog", emitLog: EmitLog.fromPartial(object.body.emitLog) };
        }
        break;
      }
      case "report": {
        if (object.body?.report !== undefined && object.body?.report !== null) {
          message.body = { $case: "report", report: EventReport.fromPartial(object.body.report) };
        }
        break;
      }
      case "ack": {
        if (object.body?.ack !== undefined && object.body?.ack !== null) {
          message.body = { $case: "ack", ack: EventAck.fromPartial(object.body.ack) };
        }
        break;
      }
      case "cancel": {
        if (object.body?.cancel !== undefined && object.body?.cancel !== null) {
          message.body = { $case: "cancel", cancel: CancelEvent.fromPartial(object.body.cancel) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseRuntimeMessage_MetadataEntry(): RuntimeMessage_MetadataEntry {
  return { key: "", value: "" };
}

export const RuntimeMessage_MetadataEntry: MessageFns<RuntimeMessage_MetadataEntry> = {
  encode(message: RuntimeMessage_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuntimeMessage_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuntimeMessage_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuntimeMessage_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: RuntimeMessage_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<RuntimeMessage_MetadataEntry>): RuntimeMessage_MetadataEntry {
    return RuntimeMessage_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RuntimeMessage_MetadataEntry>): RuntimeMessage_MetadataEntry {
    const message = createBaseRuntimeMessage_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseLifecycleEvent(): LifecycleEvent {
  return { type: 0 };
}

export const LifecycleEvent: MessageFns<LifecycleEvent> = {
  encode(message: LifecycleEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LifecycleEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLifecycleEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LifecycleEvent {
    return { type: isSet(object.type) ? lifecycleTypeFromJSON(object.type) : 0 };
  },

  toJSON(message: LifecycleEvent): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = lifecycleTypeToJSON(message.type);
    }
    return obj;
  },

  create(base?: DeepPartial<LifecycleEvent>): LifecycleEvent {
    return LifecycleEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LifecycleEvent>): LifecycleEvent {
    const message = createBaseLifecycleEvent();
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseSignalEvent(): SignalEvent {
  return { name: "", properties: Buffer.alloc(0) };
}

export const SignalEvent: MessageFns<SignalEvent> = {
  encode(message: SignalEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.properties.length !== 0) {
      writer.uint32(18).bytes(message.properties);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignalEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignalEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.properties = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignalEvent {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      properties: isSet(object.properties) ? Buffer.from(bytesFromBase64(object.properties)) : Buffer.alloc(0),
    };
  },

  toJSON(message: SignalEvent): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.properties.length !== 0) {
      obj.properties = base64FromBytes(message.properties);
    }
    return obj;
  },

  create(base?: DeepPartial<SignalEvent>): SignalEvent {
    return SignalEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SignalEvent>): SignalEvent {
    const message = createBaseSignalEvent();
    message.name = object.name ?? "";
    message.properties = object.properties ?? Buffer.alloc(0);
    return message;
  },
};

function createBasePayloadEvent(): PayloadEvent {
  return { name: "", properties: Buffer.alloc(0) };
}

export const PayloadEvent: MessageFns<PayloadEvent> = {
  encode(message: PayloadEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.properties.length !== 0) {
      writer.uint32(18).bytes(message.properties);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PayloadEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePayloadEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.properties = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PayloadEvent {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      properties: isSet(object.properties) ? Buffer.from(bytesFromBase64(object.properties)) : Buffer.alloc(0),
    };
  },

  toJSON(message: PayloadEvent): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.properties.length !== 0) {
      obj.properties = base64FromBytes(message.properties);
    }
    return obj;
  },

  create(base?: DeepPartial<PayloadEvent>): PayloadEvent {
    return PayloadEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PayloadEvent>): PayloadEvent {
    const message = createBasePayloadEvent();
    message.name = object.name ?? "";
    message.properties = object.properties ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseEmitSignal(): EmitSignal {
  return { signal: undefined, port: 0 };
}

export const EmitSignal: MessageFns<EmitSignal> = {
  encode(message: EmitSignal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signal !== undefined) {
      SignalEvent.encode(message.signal, writer.uint32(10).fork()).join();
    }
    if (message.port !== 0) {
      writer.uint32(16).int32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmitSignal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmitSignal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signal = SignalEvent.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmitSignal {
    return {
      signal: isSet(object.signal) ? SignalEvent.fromJSON(object.signal) : undefined,
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: EmitSignal): unknown {
    const obj: any = {};
    if (message.signal !== undefined) {
      obj.signal = SignalEvent.toJSON(message.signal);
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create(base?: DeepPartial<EmitSignal>): EmitSignal {
    return EmitSignal.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EmitSignal>): EmitSignal {
    const message = createBaseEmitSignal();
    message.signal = (object.signal !== undefined && object.signal !== null)
      ? SignalEvent.fromPartial(object.signal)
      : undefined;
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseEmitPayload(): EmitPayload {
  return { payload: undefined, port: 0 };
}

export const EmitPayload: MessageFns<EmitPayload> = {
  encode(message: EmitPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload !== undefined) {
      PayloadEvent.encode(message.payload, writer.uint32(10).fork()).join();
    }
    if (message.port !== 0) {
      writer.uint32(16).int32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmitPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmitPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = PayloadEvent.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmitPayload {
    return {
      payload: isSet(object.payload) ? PayloadEvent.fromJSON(object.payload) : undefined,
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: EmitPayload): unknown {
    const obj: any = {};
    if (message.payload !== undefined) {
      obj.payload = PayloadEvent.toJSON(message.payload);
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create(base?: DeepPartial<EmitPayload>): EmitPayload {
    return EmitPayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EmitPayload>): EmitPayload {
    const message = createBaseEmitPayload();
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? PayloadEvent.fromPartial(object.payload)
      : undefined;
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseEmitLog(): EmitLog {
  return { level: 0, message: "", fields: {} };
}

export const EmitLog: MessageFns<EmitLog> = {
  encode(message: EmitLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.level !== 0) {
      writer.uint32(8).int32(message.level);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    globalThis.Object.entries(message.fields).forEach(([key, value]: [string, string]) => {
      EmitLog_FieldsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmitLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmitLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.level = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = EmitLog_FieldsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.fields[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmitLog {
    return {
      level: isSet(object.level) ? logLevelFromJSON(object.level) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      fields: isObject(object.fields)
        ? (globalThis.Object.entries(object.fields) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: EmitLog): unknown {
    const obj: any = {};
    if (message.level !== 0) {
      obj.level = logLevelToJSON(message.level);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.fields) {
      const entries = globalThis.Object.entries(message.fields) as [string, string][];
      if (entries.length > 0) {
        obj.fields = {};
        entries.forEach(([k, v]) => {
          obj.fields[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<EmitLog>): EmitLog {
    return EmitLog.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EmitLog>): EmitLog {
    const message = createBaseEmitLog();
    message.level = object.level ?? 0;
    message.message = object.message ?? "";
    message.fields = (globalThis.Object.entries(object.fields ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseEmitLog_FieldsEntry(): EmitLog_FieldsEntry {
  return { key: "", value: "" };
}

export const EmitLog_FieldsEntry: MessageFns<EmitLog_FieldsEntry> = {
  encode(message: EmitLog_FieldsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmitLog_FieldsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmitLog_FieldsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmitLog_FieldsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: EmitLog_FieldsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<EmitLog_FieldsEntry>): EmitLog_FieldsEntry {
    return EmitLog_FieldsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EmitLog_FieldsEntry>): EmitLog_FieldsEntry {
    const message = createBaseEmitLog_FieldsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEventReport(): EventReport {
  return { status: 0, error: undefined };
}

export const EventReport: MessageFns<EventReport> = {
  encode(message: EventReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.error !== undefined) {
      RemoteError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = RemoteError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventReport {
    return {
      status: isSet(object.status) ? reportStatusFromJSON(object.status) : 0,
      error: isSet(object.error) ? RemoteError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: EventReport): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = reportStatusToJSON(message.status);
    }
    if (message.error !== undefined) {
      obj.error = RemoteError.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<EventReport>): EventReport {
    return EventReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventReport>): EventReport {
    const message = createBaseEventReport();
    message.status = object.status ?? 0;
    message.error = (object.error !== undefined && object.error !== null)
      ? RemoteError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseRemoteError(): RemoteError {
  return { code: "", message: "", details: "" };
}

export const RemoteError: MessageFns<RemoteError> = {
  encode(message: RemoteError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.details !== "") {
      writer.uint32(26).string(message.details);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoteError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoteError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.details = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoteError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      details: isSet(object.details) ? globalThis.String(object.details) : "",
    };
  },

  toJSON(message: RemoteError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.details !== "") {
      obj.details = message.details;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoteError>): RemoteError {
    return RemoteError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoteError>): RemoteError {
    const message = createBaseRemoteError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.details = object.details ?? "";
    return message;
  },
};

function createBaseEventAck(): EventAck {
  return { timestamp: 0 };
}

export const EventAck: MessageFns<EventAck> = {
  encode(message: EventAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== 0) {
      writer.uint32(8).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAck {
    return { timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0 };
  },

  toJSON(message: EventAck): unknown {
    const obj: any = {};
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create(base?: DeepPartial<EventAck>): EventAck {
    return EventAck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventAck>): EventAck {
    const message = createBaseEventAck();
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseCancelEvent(): CancelEvent {
  return {};
}

export const CancelEvent: MessageFns<CancelEvent> = {
  encode(_: CancelEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CancelEvent {
    return {};
  },

  toJSON(_: CancelEvent): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CancelEvent>): CancelEvent {
    return CancelEvent.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CancelEvent>): CancelEvent {
    const message = createBaseCancelEvent();
    return message;
  },
};

/** RemotePluginService is implemented by a remote plugin server. */
export type RemotePluginServiceService = typeof RemotePluginServiceService;
export const RemotePluginServiceService = {
  /** Ping is a lightweight unary heartbeat request. */
  ping: {
    path: "/plugin.v1.RemotePluginService/Ping" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PingRequest): Buffer => Buffer.from(PingRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PingRequest => PingRequest.decode(value),
    responseSerialize: (value: PingResponse): Buffer => Buffer.from(PingResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PingResponse => PingResponse.decode(value),
  },
  /** ListPlugins returns plugin metadata exposed by the remote plugin server. */
  listPlugins: {
    path: "/plugin.v1.RemotePluginService/ListPlugins" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListPluginsRequest): Buffer => Buffer.from(ListPluginsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListPluginsRequest => ListPluginsRequest.decode(value),
    responseSerialize: (value: ListPluginsResponse): Buffer => Buffer.from(ListPluginsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListPluginsResponse => ListPluginsResponse.decode(value),
  },
  /** CreateInstance creates one plugin instance for a workflow node/session. */
  createInstance: {
    path: "/plugin.v1.RemotePluginService/CreateInstance" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateInstanceRequest): Buffer =>
      Buffer.from(CreateInstanceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateInstanceRequest => CreateInstanceRequest.decode(value),
    responseSerialize: (value: CreateInstanceResponse): Buffer =>
      Buffer.from(CreateInstanceResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateInstanceResponse => CreateInstanceResponse.decode(value),
  },
  /** DestroyInstance releases a previously created plugin instance. */
  destroyInstance: {
    path: "/plugin.v1.RemotePluginService/DestroyInstance" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DestroyInstanceRequest): Buffer =>
      Buffer.from(DestroyInstanceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DestroyInstanceRequest => DestroyInstanceRequest.decode(value),
    responseSerialize: (value: DestroyInstanceResponse): Buffer =>
      Buffer.from(DestroyInstanceResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DestroyInstanceResponse => DestroyInstanceResponse.decode(value),
  },
  /** RunInstance is the dedicated bidirectional event stream for one plugin instance. */
  runInstance: {
    path: "/plugin.v1.RemotePluginService/RunInstance" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: RuntimeMessage): Buffer => Buffer.from(RuntimeMessage.encode(value).finish()),
    requestDeserialize: (value: Buffer): RuntimeMessage => RuntimeMessage.decode(value),
    responseSerialize: (value: RuntimeMessage): Buffer => Buffer.from(RuntimeMessage.encode(value).finish()),
    responseDeserialize: (value: Buffer): RuntimeMessage => RuntimeMessage.decode(value),
  },
} as const;

export interface RemotePluginServiceServer extends UntypedServiceImplementation {
  /** Ping is a lightweight unary heartbeat request. */
  ping: handleUnaryCall<PingRequest, PingResponse>;
  /** ListPlugins returns plugin metadata exposed by the remote plugin server. */
  listPlugins: handleUnaryCall<ListPluginsRequest, ListPluginsResponse>;
  /** CreateInstance creates one plugin instance for a workflow node/session. */
  createInstance: handleUnaryCall<CreateInstanceRequest, CreateInstanceResponse>;
  /** DestroyInstance releases a previously created plugin instance. */
  destroyInstance: handleUnaryCall<DestroyInstanceRequest, DestroyInstanceResponse>;
  /** RunInstance is the dedicated bidirectional event stream for one plugin instance. */
  runInstance: handleBidiStreamingCall<RuntimeMessage, RuntimeMessage>;
}

export interface RemotePluginServiceClient extends Client {
  /** Ping is a lightweight unary heartbeat request. */
  ping(request: PingRequest, callback: (error: ServiceError | null, response: PingResponse) => void): ClientUnaryCall;
  ping(
    request: PingRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PingResponse) => void,
  ): ClientUnaryCall;
  ping(
    request: PingRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PingResponse) => void,
  ): ClientUnaryCall;
  /** ListPlugins returns plugin metadata exposed by the remote plugin server. */
  listPlugins(
    request: ListPluginsRequest,
    callback: (error: ServiceError | null, response: ListPluginsResponse) => void,
  ): ClientUnaryCall;
  listPlugins(
    request: ListPluginsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListPluginsResponse) => void,
  ): ClientUnaryCall;
  listPlugins(
    request: ListPluginsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListPluginsResponse) => void,
  ): ClientUnaryCall;
  /** CreateInstance creates one plugin instance for a workflow node/session. */
  createInstance(
    request: CreateInstanceRequest,
    callback: (error: ServiceError | null, response: CreateInstanceResponse) => void,
  ): ClientUnaryCall;
  createInstance(
    request: CreateInstanceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateInstanceResponse) => void,
  ): ClientUnaryCall;
  createInstance(
    request: CreateInstanceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateInstanceResponse) => void,
  ): ClientUnaryCall;
  /** DestroyInstance releases a previously created plugin instance. */
  destroyInstance(
    request: DestroyInstanceRequest,
    callback: (error: ServiceError | null, response: DestroyInstanceResponse) => void,
  ): ClientUnaryCall;
  destroyInstance(
    request: DestroyInstanceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DestroyInstanceResponse) => void,
  ): ClientUnaryCall;
  destroyInstance(
    request: DestroyInstanceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DestroyInstanceResponse) => void,
  ): ClientUnaryCall;
  /** RunInstance is the dedicated bidirectional event stream for one plugin instance. */
  runInstance(): ClientDuplexStream<RuntimeMessage, RuntimeMessage>;
  runInstance(options: Partial<CallOptions>): ClientDuplexStream<RuntimeMessage, RuntimeMessage>;
  runInstance(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<RuntimeMessage, RuntimeMessage>;
}

export const RemotePluginServiceClient = makeGenericClientConstructor(
  RemotePluginServiceService,
  "plugin.v1.RemotePluginService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): RemotePluginServiceClient;
  service: typeof RemotePluginServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
